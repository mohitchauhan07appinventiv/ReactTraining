modules->login, home, signup, dashboard, 
component -> 

--Day 2
* There are 2 ways to crete a reusable component.
    1. if reusableility is in same file then create a function in same file and reuse.
    2. if reusableility is in different files then create a new file and reuse.

Optional parameters will be passed in the last in the arguments

all reusable 

props flow is from pareent to child
react fragment


----------------------------------
*Props immutable i.e not setState like method
*state update using setState method in clas components only

For Promise :- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise

problem in setting state 
https://codesandbox.io/s/react-new?file=/src/App.js

using validation function and saperate error state
https://codesandbox.io/s/new-state-object-for-errors-and-validation-function-emti5?file=/src/App.js